<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Gateway (REST) puis voiture en direct (UDP/TCP), toujours sur le
         réseau local (docs/mobile-protocol.md) : pas d'autre permission
         réseau requise tant qu'il n'y a pas de découverte Wi-Fi Direct. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Le protocole entier (Gateway REST, contrôle/télémétrie/vidéo P2P,
         docs/mobile-protocol.md) est en clair par conception, toujours sur
         le réseau local — jamais de TLS. Sans usesCleartextTraffic, Android
         (API 28+) bloque silencieusement tout appel http:// : le Gateway
         paraît "injoignable" alors qu'aucune requête ne part réellement. -->
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.SmartCarPilot"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
